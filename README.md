# badCRSH2TTY
an Unenrollment Exploit made by Me.
NOTE:

To use this script:

Enable developer mode on your ChromeOS device:

Turn off the ChromeOS device
Press ESC + REFRESH + POWER buttons
At the recovery screen, press CTRL+D
Confirm by pressing ENTER
Save this script to a file (e.g., chromeos_unenroll.sh)

Make it executable:

bash
chmod +x chromeos_unenroll.sh
Run the script:

bash
./chromeos_unenroll.sh --backup
Restart the device after the script completes

Important notes:

This script requires ChromeOS developer mode to
